[project]
name = "adaptivetemperaturerecsys"
version = "0.1.0"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "matplotlib>=3.10.8",
    "dataset",
    "model",
    "comet_ml>=3.0.0",
    "datasets>=4.5.0",
    "pandas>=3.0.0",
    "polars>=1.38.1",
    "torch>=2.10.0",
    "ipykernel>=7.1.0",
    "jupyter>=1.1.1",
    "pandas-stubs~=3.0.0",
]

[tool.uv.workspace]
members = [
    "dataset",
    "model"
]

[tool.uv.sources]
dataset = { workspace = true }
model = { workspace = true }

