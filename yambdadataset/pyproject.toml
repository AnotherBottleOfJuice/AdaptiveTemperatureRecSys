[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "yambdadataset"
version = "0.1.0"
dependencies = []

[tool.setuptools]
packages = ["yambdadataset"] 
package-dir = {"yambdadataset" = "."}